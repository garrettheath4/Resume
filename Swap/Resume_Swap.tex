% Garrett Heath Koller Resume -- Swap Senior Software Engineer application
% vim: set textwidth=120 tabstop=4 softtabstop=4 expandtab smarttab shiftround autoindent smartindent:
\documentclass[11pt, letterpaper]{letter}
\input{resume_preamble}
\input{resume_toggle_defaults}
\togglefalse{goals}          % replaced by swapgoals
\toggletrue{swapgoals}       % TypeScript/Node.js full-stack depth is the strongest match for this role
\toggletrue{swapskills}      % surfaces MongoDB & Redis, honestly scoped as personal-project experience only
% aitools left off: Swap's posting doesn't call out AI-assisted engineering
% bahconsultant=true by default: enterprise consulting context adds breadth, not a strong fit but not a detractor
% wluresearch=true by default: HPC background not central but doesn't hurt
% languages=true by default: Dutch (A2) is a meaningful signal for a Netherlands-based role
% highlightlgbt=true by default
% sumwluleadership=true by default (keeps WLU section concise)
\begin{document}
\input{resume_content}
\end{document}
