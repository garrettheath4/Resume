% Garrett Heath Koller Resume -- JetBrains Principal Forward Deployed Engineer application
% vim: set textwidth=120 tabstop=4 softtabstop=4 expandtab smarttab shiftround autoindent smartindent:
\documentclass[11pt, letterpaper]{letter}
\input{resume_preamble}
\input{resume_toggle_defaults}
\togglefalse{goals}            % replaced by jetbrainsgoals
\toggletrue{jetbrainsgoals}    % IntelliJ fluency, enterprise access-control work, AI-assisted engineering
\toggletrue{aitools}           % role values AI-assisted engineering; honest signal as a power-user (not an agent builder)
% bahconsultant=true by default: enterprise/regulated-environment consulting context is directly relevant to FDE work
% wluresearch=true by default: HPC background adds depth, though not central to this role
% languages=true by default: Dutch (A2) is a meaningful signal for Amsterdam-based role
% highlightlgbt=true by default
% sumwluleadership=true by default (keeps WLU section concise)
\begin{document}
\input{resume_content}
\end{document}
